

@font-face
{
font-family: calligraphy;
src: url('/fonts/calligraphy.ttf')
}


// 修改首页标题
div.home-blog .hero h1,.loading-wrapper .title,.password-shadow .title {
  font-family: calligraphy;
  text-align: center;
  font-size: 6rem;
}

// 修改左上角名称字体
.navbar span.site-name {
  font-family: calligraphy;
  font-weight: 400;
  font-size: 2rem;
  line-height: 2rem;
  background-image: linear-gradient($accentColor, $auxiliarycolor)
  -webkit-background-clip: text;
  color: transparent;
}

// 隐藏任务列表默认 li 样式
.contains-task-list li {
  list-style-type:none;
}